<header class="bg-white border-b border-black/5">
  <div class="w-full mx-auto lg:px-12 px-8 max-w-7xl lg:px-16">
    <div
      class="relative flex flex-col w-full py-5 mx-auto bg-white lg:items-center lg:justify-between lg:flex-row lg:px-6">
      <div class="flex flex-row items-center justify-between lg:justify-start">
        <a href="/" class="text-black inline-flex items-center gap-3">
          <img width="40" src="/logo.svg" alt="logo" />
          <span class="font-bold font-display">Process</span>
        </a>
        <button
          id="mobile-menu-toggle"
          aria-expanded="false"
          aria-controls="main-navigation"
          aria-label="Toggle navigation menu"
          class="inline-flex items-center justify-center p-2 text-base-400 hover:text-black focus:outline-none focus:text-black lg:hidden">
          <svg
            id="mobile-menu-icon"
            class="w-6 h-6 transition-transform duration-300 ease-in-out"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24">
            <path
              id="menu-icon-path"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16">
            </path>
          </svg>
        </button>
      </div>
      <nav
        id="main-navigation"
        class="flex-col items-center flex-grow gap-4 lg:gap-8 flex overflow-hidden lg:pb-0 lg:flex lg:justify-end lg:h-auto lg:flex-row h-0 lg:h-auto transition-[height] duration-300"
        style="height: 0;"
      >
        <a
          class="text-sm text-black transition duration-300 hover:text-primary lg:ml-auto"
          href="/#features">
          Features
        </a>
        <a
          href="/pricing"
          class="text-sm text-black transition duration-300 hover:text-primary relative">
          Pricing
        </a>
        <a
          href="/waitlist"
          class="text-sm text-black transition duration-300 hover:text-primary relative">
          Waitlist
          <span
            class="bg-blue-500 ms-1 text-white text-[10px] px-2 py-1 rounded-full">
            JOIN
          </span>
        </a>
        <a
          href="/faq"
          class="text-sm text-black transition duration-300 hover:text-primary relative">
          FAQ
        </a>
        <div class="inline-flex items-center gap-2 list-none py-px lg:ml-auto">
          <a
            href="/waitlist"
            class="flex items-center justify-center transition px-4 py-2 w-full sm:w-auto rounded-md bg-primary-light text-white hover:bg-primary text-xs font-medium h-8">
            Sign up
          </a>
          <a
            href="/login"
            class="flex items-center justify-center transition-all duration-200 focus:ring-2 focus:outline-none text-base-700 bg-base-50 hover:bg-white focus:ring-base-500/50 h-8 px-4 py-2 text-xs font-medium rounded-md">
            Log in
          </a>
        </div>
      </nav>
    </div>
  </div>
  <script type="text/javascript">
    (() => {
      const menuToggle = document.getElementById('mobile-menu-toggle');
      const nav = document.getElementById('main-navigation');
      const menuIconPath = document.getElementById('menu-icon-path');
      let isOpen = false;

      // Helper to update menu state
      function setMenu(open) {
        isOpen = open;
        menuToggle.setAttribute('aria-expanded', open.toString());
        if (open) {
          nav.style.height = nav.scrollHeight + 'px';
          menuIconPath.setAttribute('d', 'M6 18L18 6M6 6l12 12'); // X icon
        } else {
          nav.style.height = '0';
          menuIconPath.setAttribute('d', 'M4 6h16M4 12h16M4 18h16'); // Hamburger
        }
      }

      // Toggle menu on button click
      menuToggle.addEventListener('click', (e) => {
        e.stopPropagation();
        setMenu(!isOpen);
      });

      // Close menu when clicking a nav link (on mobile)
      nav.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', () => {
          if (window.innerWidth < 1024) setMenu(false);
        });
      });

      // Close menu when clicking outside
      document.addEventListener('click', (e) => {
        if (isOpen && window.innerWidth < 1024) {
          if (!nav.contains(e.target) && !menuToggle.contains(e.target)) {
            setMenu(false);
          }
        }
      });

      // Responsive: close menu on resize to desktop
      window.addEventListener('resize', () => {
        if (window.innerWidth >= 1024) setMenu(false);
      });
    })();
  </script>
</header>